import { TypeValidatorDirective } from './type-validator.directive';

describe('TypeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
